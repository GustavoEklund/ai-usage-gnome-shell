{
  "name": "ai-usage-gnome-shell",
  "version": "0.1.1",
  "description": "GNOME Shell extension showing Claude Code usage limits and token history in the top bar",
  "type": "module",
  "private": true,
  "license": "GPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/GustavoEklund/ai-usage-gnome-shell.git"
  },
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-jsdoc": "^50.0.0",
    "vitest": "^2.0.0"
  }
}
